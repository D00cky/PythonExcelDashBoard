# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

PythonExcelDashBoard — a Python-based Excel dashboard project (early stage, no source code yet).

## Sandbox

This project runs inside [ai-jail](https://github.com/akitaonrails/ai-jail). The `.ai-jail` file configures the sandbox (read/write path maps). Regenerate it with:

```
ai-jail --clean --init
```
